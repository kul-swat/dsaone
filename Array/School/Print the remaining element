A[] = {7, 8, 3, 4, 2, 9, 5}
Ouput:
5

def leftElement(self, arr, n):
    # Your code goes here  
        arr.sort()
        if n%2==0:
          return arr[n//2-1]
        else:
          return arr[n//2]
